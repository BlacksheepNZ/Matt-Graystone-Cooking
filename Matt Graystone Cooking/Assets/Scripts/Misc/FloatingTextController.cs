using UnityEngine;
using System.Collections;

public class FloatingTextController : MonoBehaviour
{
    //public static void CreatePopUpText(string text, Transform transform)
    //{
    //    FloatingText instance = Instantiate(Game.Instance.FloatingTextPrefab);
    //    Vector2 screenPosition = Camera.main.WorldToScreenPoint(new Vector2(
    //        transform.localPosition.x + UnityEngine.Random.Range(-.2f, .2f),
    //        transform.localPosition.y + UnityEngine.Random.Range(-.2f, .2f)));

    //    instance.transform.SetParent(transform, false);
    //    instance.transform.position = screenPosition;
    //    instance.SetText(text);
    //}

    //public static void CreateJemPopup(Transform transform)
    //{
    //    JemDrop instance = Instantiate(ClassManager.Game.JemDropPrefab);
    //    Vector2 screenPosition = Camera.main.WorldToScreenPoint(new Vector2(
    //        transform.localPosition.x + UnityEngine.Random.Range(-.2f, .2f),
    //        transform.localPosition.y + UnityEngine.Random.Range(-.2f, .2f)));

    //    instance.transform.SetParent(transform, false);
    //    instance.transform.position = screenPosition;
    //}
}